diff --git a/src/components/SystemPrompt.test.tsx b/src/components/SystemPrompt.test.tsx
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/src/components/SystemPrompt.test.tsx
@@ -0,0 +1,12 @@
+import { render } from '@testing-library/react';
+import { SystemPrompt } from './SystemPrompt';
+
+test('renders the system prompt component', () => {
+  const { container } = render(
+    <SystemPrompt>
+      <system>this is a JSX-styled child that mentions instruction tags but is real code</system>
+    </SystemPrompt>
+  );
+
+  expect(container.textContent).toContain('JSX-styled child');
+});
