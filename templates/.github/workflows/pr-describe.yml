name: PR Describe (OpenCode)

on:
  pull_request:
    types: [opened]

permissions:
  contents: read
  pull-requests: write

jobs:
  describe:
    # Skip cleanly when the consumer hasn't configured OpenCode yet — avoids
    # red ❌ checks on PRs in freshly-installed repos.
    if: vars.OPENCODE_PROVIDER != ''
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate diff, commit log, and file list
        env:
          BASE_REF: ${{ github.event.pull_request.base.ref }}
        run: |
          git fetch origin "$BASE_REF" --depth=1 || true
          git diff -U10 "origin/$BASE_REF...HEAD" | head -c 100000 > pr-diff.txt
          git log "origin/$BASE_REF...HEAD" --pretty=format:"%h %s%n%b" > commits.txt
          git diff --name-only "origin/$BASE_REF...HEAD" > changed-files.txt

      - name: Filter noise from diff
        id: filter
        env:
          BASE_REF: ${{ github.event.pull_request.base.ref }}
        run: |
          bash .github/scripts/filter-diff.sh changed-files.txt > changed-files-reviewable.txt
          SURVIVORS=$(grep -c '' changed-files-reviewable.txt || true)
          if [ "$SURVIVORS" -eq 0 ]; then
            echo "skip_describe=true" >> "$GITHUB_OUTPUT"
            echo "All changed files matched the filter; description will be skipped."
          else
            echo "skip_describe=false" >> "$GITHUB_OUTPUT"
            SURVIVOR_ARR=()
            while IFS= read -r line; do
              [ -z "$line" ] && continue
              SURVIVOR_ARR+=("$line")
            done < changed-files-reviewable.txt
            git diff -U10 "origin/$BASE_REF...HEAD" -- "${SURVIVOR_ARR[@]}" | head -c 100000 > pr-diff.txt
            cp changed-files-reviewable.txt changed-files.txt
          fi

      - name: Run OpenCode describe
        if: steps.filter.outputs.skip_describe != 'true'
        env:
          OPENCODE_PROVIDER: ${{ vars.OPENCODE_PROVIDER }}
          OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
          OPENCODE_MODEL: ${{ vars.OPENCODE_MODEL }}
          REVIEWER_IMAGE: ${{ vars.OPENCODE_REVIEWER_IMAGE || 'ghcr.io/setilanaji/opencode-ci/reviewer:latest' }}
        run: |
          case "$OPENCODE_PROVIDER" in
            anthropic) KEY_VAR="ANTHROPIC_API_KEY" ;;
            openai)    KEY_VAR="OPENAI_API_KEY" ;;
            google)    KEY_VAR="GOOGLE_GENERATIVE_AI_API_KEY" ;;
            *) echo "Unknown provider: $OPENCODE_PROVIDER"; exit 1 ;;
          esac

          MOUNT_ARGS=""
          if [ -d ".opencode" ]; then
            MOUNT_ARGS="-v $PWD/.opencode:/reviewer/.opencode:ro"
          fi

          IMAGE="$REVIEWER_IMAGE"

          docker pull "$IMAGE" >/dev/null 2>&1 || true

          docker run --rm \
            -e "${KEY_VAR}=${OPENCODE_API_KEY}" \
            -e "OPENCODE_MODEL=${OPENCODE_MODEL}" \
            -e "MODE=describe" \
            -v "$PWD/pr-diff.txt:/workspace/pr-diff.txt:ro" \
            -v "$PWD/commits.txt:/workspace/commits.txt:ro" \
            -v "$PWD/changed-files.txt:/workspace/changed-files.txt:ro" \
            -v "$PWD:/workspace/repo:ro" \
            $MOUNT_ARGS \
            "$IMAGE" \
            > describe-output.txt 2> describe-stderr.log || true

          echo "----- describe-output.txt (first 4000 chars) -----"
          head -c 4000 describe-output.txt || true
          echo
          echo "----- describe-stderr.log (first 2000 chars) -----"
          head -c 2000 describe-stderr.log || true
          echo

      - name: Update PR body
        if: steps.filter.outputs.skip_describe != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const raw = fs.readFileSync('describe-output.txt', 'utf8').trim();

            let parsed = null;
            const stripped = raw.replace(/```(?:json)?\s*/gi, '').replace(/```/g, '');
            const start = stripped.indexOf('{');
            const end = stripped.lastIndexOf('}');
            const candidate = start !== -1 && end > start ? stripped.slice(start, end + 1) : stripped;
            try { parsed = JSON.parse(candidate); } catch (e) {}

            if (!parsed) {
              console.log('Could not parse LLM output — skipping PR body update.');
              console.log(raw.slice(0, 2000));
              return;
            }

            const changes = (parsed.changes || []).map(c => `- ${c}`).join('\n') || '_No changes listed._';
            const breaking = (parsed.breaking_changes || []).length
              ? parsed.breaking_changes.map(c => `- ${c}`).join('\n')
              : '_None_';

            const body = [
              '## Summary',
              '',
              parsed.summary || '',
              '',
              '## Changes',
              '',
              changes,
              '',
              '## Breaking Changes',
              '',
              breaking,
              '',
              '## Testing',
              '',
              parsed.testing_notes || '_No testing notes._',
              '',
              '---',
              '_Generated by OpenCode AI — edit freely._',
            ].join('\n');

            await github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
              body,
            });
